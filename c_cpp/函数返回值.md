### 返回非引用
    函数的返回值用于初始化在跳出函数时候创建的`临时变量`, 如果返回类型不是引用的话, 在函数返回的地方会将返回值`复制`给临时对象. 因此其返回值可以是局部变量, 也可以是表达式结果. 
    值得注意的是将返回值复制给临时对象时候, 会有很大的内存消耗

### 返回引用
    直接将返回对象的引用(即对象本身), 这样的返回值是左值可以出现在赋值号的左边, 这就要求在函数返回之后被引用的对象依然存在, 不能是在函数内部临时创建的临时变量的引用.
    由于没有了复制, 效率自然就会比返回非引用要高很多. 

